#!/usr/bin/env bash
# Run the WarpX 3D solver.
. "$(dirname "${BASH_SOURCE[0]}")/warpx-env.sh"

root=$(warpx_root)
bin="$root/vendor/warpx/build/bin/warpx.3d"

if [ ! -x "$bin" ]; then
    echo "warpx3d: not built — run warpx-build" >&2
    exit 1
fi

exec "$bin" "$@"
